quit
@map[key].prev
@map[key].next
c
@map[key].prev
@map[key].next
c
@map[key].prev
@map[key].next
c
@map[key].prev
@map[key].next
@map[key]
display @map.include?(key)
c
@map.include?(key)
c
@map.include?(key)
c
@map.include?(key)
@map.include(key)
display key
